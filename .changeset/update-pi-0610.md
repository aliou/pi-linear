---
"@aliou/pi-linear": patch
---

Update Pi dependencies to 0.61.0. Custom tools now declare `promptSnippet` so they remain visible in Pi's default tool prompt, and action enums use `StringEnum` for better provider compatibility.
